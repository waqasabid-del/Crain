"""CAIRN API — FastAPI backend.

Populated in Step 9 (md/16-build-steps.md). This package exists now so the uv
workspace resolves and CI has something to lint, typecheck and test.
"""

__version__ = "0.0.0"
